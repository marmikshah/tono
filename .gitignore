/target
/dist
.DS_Store
*.swp
/crates/tono-desktop/gen/

# A library resolves at the consumer's end, so the lockfile is not committed.
/Cargo.lock

# Local-only notes kept out of the repo
/docs/architecture.html
