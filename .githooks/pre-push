#!/bin/sh
# Pre-push gate: run the exact CI steps locally so a push can never burn a
# failing Actions run. It just calls the Makefile target, so the gate lives in
# exactly one place. Enable once per clone with:
#   git config core.hooksPath .githooks
set -e
exec make verify
